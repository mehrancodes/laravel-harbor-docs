[build]
command = "composer install --no-dev --optimize-autoloader --no-interaction && npm ci --include=dev && npm run build"
publish = "build_production"

[build.environment]
NODE_VERSION = "22"
PHP_VERSION = "8.3"
NPM_FLAGS = "--include=dev"
